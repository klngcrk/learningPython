# 1*.Napisz program, który w zmiennej będzie przechowywał hasło i który po uruchomieniu będzie prosił użytkownika o podanie hasła i nie pozwoli przejść dalej,
# jeśli użytkownik nie poda właściwego hasła.